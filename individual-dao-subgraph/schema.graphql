type App @entity {
    id: ID! # appID from SetApp (Ikernal.sol)
    namespace: [Bytes!]
    appName: String
    appAddress: [Bytes!]
    proxyAddress: [Bytes!]
    upgradeable: Boolean


    entity: [Bytes!]
#    role: [Bytes!]
#    allowed: [Boolean!]
#    params: [Bytes!]
}

type ACL @entity {
    id: ID! # acl address

}

type EVMScriptRegistry @entity {
    id: ID! # evm script reg address
}

type Kernel @entity {
    id: ID! # kernel address
}

type TokenManager @entity {
    id: ID! # tokenManager address
}

type Voting @entity {
    id: ID! # voting address
}

type Finance @entity {
    id: ID! # finance address
}

type Vault @entity {
    id: ID! # vault address
}