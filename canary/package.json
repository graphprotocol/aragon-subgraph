{
  "name": "aragon-subgraph",
  "license": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ graphprotocol/aragon",
    "create-local": "graph create --node http://localhost:8020/ graphprotocol/aragon",
    "remove-local": "graph remove --node http://localhost:8020/ graphprotocol/aragon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 graphprotocol/aragon"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.16.0",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}
